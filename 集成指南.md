# 快速集成指南

本指南帮助你将改进的读数计算算法集成到现有的 `app.py` 中。

---

## 方案A：完全替换（推荐用于测试环境）

### 步骤1：备份原文件
```bash
cp app.py app_backup.py
```

### 步骤2：安装依赖
```bash
pip install scipy scikit-learn
```

### 步骤3：使用改进版
```bash
cp app_improved.py app.py
```

### 步骤4：复制缺失的路由
改进版只包含核心检测逻辑，需要从备份文件复制：
- 所有 `@app.route` 装饰的函数（除了已有的）
- `init_db()` 完整实现
- 其他业务逻辑函数

---

## 方案B：逐步集成（推荐用于生产环境）

### 第1步：添加新依赖（文件顶部）

在原 `app.py` 中添加导入：
```python
from scipy.spatial.distance import mahalanobis
from scipy.optimize import least_squares
from sklearn.linear_model import RANSACRegressor
```

### 第2步：添加滤波器类（在数学工具函数区域）

复制以下类到 `app.py` 中：
```python
class KalmanFilter1D:
    """一维卡尔曼滤波器"""
    def __init__(self, process_variance=1e-5, measurement_variance=1e-2):
        self.process_variance = process_variance
        self.measurement_variance = measurement_variance
        self.x = 0.0
        self.P = 1.0
        self.initialized = False
    
    def update(self, measurement):
        if not self.initialized:
            self.x = measurement
            self.initialized = True
            return self.x
        
        x_pred = self.x
        P_pred = self.P + self.process_variance
        K = P_pred / (P_pred + self.measurement_variance)
        self.x = x_pred + K * (measurement - x_pred)
        self.P = (1 - K) * P_pred
        return self.x


class LowPassFilter1D:
    """一阶低通滤波器"""
    def __init__(self, alpha=0.3):
        self.alpha = alpha
        self.value = None
    
    def update(self, measurement):
        if self.value is None:
            self.value = measurement
        else:
            self.value = self.alpha * measurement + (1 - self.alpha) * self.value
        return self.value
```

### 第3步：添加全局滤波器管理

在 `_task_states` 定义后添加：
```python
# 全局滤波器实例
_filters = {}

def get_filters(task_id):
    """获取或创建任务的滤波器"""
    if task_id not in _filters:
        _filters[task_id] = {
            'kalman_cx': KalmanFilter1D(),
            'kalman_cy': KalmanFilter1D(),
            'lowpass_cx': LowPassFilter1D(alpha=0.4),
            'lowpass_cy': LowPassFilter1D(alpha=0.4),
        }
    return _filters[task_id]
```

### 第4步：添加新工具函数

将以下函数添加到 `app.py` 的数学工具区域（在 `mid_point` 之后）：

```python
# 复制以下所有函数：
- ransac_circle_center()
- compute_incenter()
- least_squares_circle_center()
- compute_angle_variance()
- dynamic_weight_fusion()
- mahalanobis_outlier_detection()
- advanced_circle_estimation()
- least_squares_reading_calibration()
- check_confidence_and_angle_anomaly()
```

### 第5步：替换 `_run_detection` 中的读数计算部分

找到原代码中的这部分（约504-551行）：
```python
# 三对直线两两求交点，取最近两个的中点作为圆心估计
ise = calc_intersection(p1, p2, p3, p4)
...
final = rv2 + (0.4 - rv3) / 2
```

**替换为改进版本：**
```python
# ===== 改进的圆心估计 =====
line_pairs = [
    ((p1, p2), (p3, p4)),  # SE
    ((p1, p2), (p5, p6)),  # SP
    ((p1, p2), (p7, p8)),  # SZ
    ((p3, p4), (p5, p6)),  # EP
    ((p3, p4), (p7, p8)),  # EZ
    ((p5, p6), (p7, p8)),  # PZ
]

center, quality_metrics = advanced_circle_estimation(line_pairs)

if center is None:
    log("圆心估计失败，使用备用方法")
    ise = calc_intersection(p1, p2, p3, p4)
    isp = calc_intersection(p1, p2, p5, p6)
    isz = calc_intersection(p1, p2, p7, p8)
    valid_intersections = [pt for pt in [ise, isp, isz] if pt is not None]
    if valid_intersections:
        center = tuple(np.mean(valid_intersections, axis=0))
    else:
        center = ((xsf + xef + xpf + xzf) / 4, (ysf + yef + ypf + yzf) / 4)

cx, cy = center

# ===== 应用卡尔曼滤波 =====
filters = get_filters(task_id)
cx_filtered = filters['kalman_cx'].update(cx)
cy_filtered = filters['kalman_cy'].update(cy)
cx, cy = cx_filtered, cy_filtered

# ===== 置信度检测 =====
alarm_level, alarm_msg = check_confidence_and_angle_anomaly(quality_metrics)
log(f"检测质量: {alarm_level} - {alarm_msg}")

# ===== 改进的读数计算 =====
angle_pointer_rel = clock_angle([xsf - cx, ysf - cy], [xpf - cx, ypf - cy])
angle_end_rel = clock_angle([xsf - cx, ysf - cy], [xef - cx, yef - cy])
angle_zero_rel = clock_angle([xsf - cx, ysf - cy], [xzf - cx, yzf - cy])

slider_position = 0.5
keypoint_angles = [0.0, angle_pointer_rel, angle_end_rel, angle_zero_rel]
reference_readings = [0.0, 1.0]

calibrated_reading = least_squares_reading_calibration(
    keypoint_angles, reference_readings, slider_position
)

rv1_original = angle_pointer_rel / angle_end_rel if angle_end_rel > 1e-6 else 0.0
final = calibrated_reading

log(f"原始读数: {rv1_original:.6f}")
log(f"校正读数: {final:.6f}")
log(f"质量指标: {quality_metrics}")
```

### 第6步：更新可视化部分

替换画圆代码部分：
```python
# 在图上标注
image_annotated = image.copy()
cv2.circle(image_annotated, (int(cx), int(cy)), 8, (0, 255, 0), -1)  # 圆心
cv2.circle(image_annotated, (int(xsf), int(ysf)), 5, (255, 0, 0), -1)  # 起点
cv2.circle(image_annotated, (int(xef), int(yef)), 5, (0, 0, 255), -1)  # 终点
cv2.circle(image_annotated, (int(xpf), int(ypf)), 5, (0, 255, 255), -1)  # 指针
cv2.circle(image_annotated, (int(xzf), int(yzf)), 5, (255, 255, 0), -1)  # 零点

# 标注候选圆心
if "candidate_centers" in quality_metrics:
    for cand in quality_metrics["candidate_centers"]:
        cv2.circle(image_annotated, (int(cand[0]), int(cand[1])), 4, (128, 128, 128), 1)

# 保存
fit_path = os.path.join(rimg, image_name + "_fitting_improved.jpg")
cv2.imwrite(fit_path, image_annotated)
state["img_fitting"] = _fwdpath(fit_path)
```

### 第7步：更新日志记录

在写入日志的部分添加质量信息：
```python
entry = (f"{now.strftime('%Y-%m-%d %H:%M:%S')}\n"
        f"任务编号：{task_id}  操作人：{op}\n"
        f"{image_name} {snum}\n"
        f"原始读数：{rv1_original:.6f}\n"
        f"校正读数：{final:.6f}\n"
        f"检测质量：{alarm_level} - {alarm_msg}\n"
        f"置信度：{quality_metrics.get('ransac_confidence', 0):.2f}\n\n")
```

### 第8步：更新数据库记录

在 `db_update_yolo` 调用中添加质量信息：
```python
db_update_yolo(task_id, 
               serial_number=snum, 
               reading_before=round(rv1_original, 6),
               reading_after=round(final, 6), 
               detect_status="success",
               detected_at=now.strftime("%Y-%m-%d %H:%M:%S"))

# 如果数据库有质量字段，可以添加：
# quality_json = json.dumps(quality_metrics)
# db_update_yolo(task_id, quality_metrics=quality_json, alarm_level=alarm_level)
```

### 第9步：测试

```bash
# 启动服务
python app.py

# 上传测试图片，检查：
# 1. 日志中是否有"检测质量"信息
# 2. 拟合图中是否有多个候选圆心标注
# 3. 读数精度是否提升
```

---

## 方案C：渐进式部署（最安全）

### 阶段1：仅启用RANSAC（第1-2周）
- 只添加 `ransac_circle_center()` 和 `advanced_circle_estimation()`
- 观察精度变化

### 阶段2：添加滤波（第3-4周）
- 添加卡尔曼/低通滤波
- 监控稳定性

### 阶段3：动态权重融合（第5-6周）
- 启用 `dynamic_weight_fusion()`
- 对比不同权重配置

### 阶段4：完整替换（第7-8周）
- 替换读数校正算法
- 全面评估

---

## 验证检查清单

- [ ] 依赖安装成功（scipy, scikit-learn）
- [ ] 代码无语法错误
- [ ] 服务正常启动
- [ ] 上传图片能正常检测
- [ ] 日志中有质量指标输出
- [ ] 拟合图生成正常
- [ ] 读数精度未下降（建议提升）
- [ ] 没有新的异常报错

---

## 回滚方案

如果出现问题：
```bash
# 立即回滚
cp app_backup.py app.py
# 重启服务
```

---

## 参数微调建议

测试后可根据实际情况调整：

```python
# 1. RANSAC严格程度
ransac_circle_center(
    intersections,
    threshold=10.0,  # 降低→更严格，提高→更宽松
    max_trials=100
)

# 2. 滤波平滑度
KalmanFilter1D(
    process_variance=1e-5,      # 降低→响应慢
    measurement_variance=1e-2    # 提高→平滑强
)

# 3. 权重比例
weights = (
    0.4 * conf_norm +          # 可调整
    0.3 * angle_score_norm +   # 可调整
    0.3 * dispersion_score_norm # 可调整
)

# 4. 报警阈值
check_confidence_and_angle_anomaly(
    quality_metrics,
    angle_variance_threshold=0.5,    # 可调整
    confidence_threshold=0.5          # 可调整
)
```

---

## 常见问题

### Q: 导入失败 "No module named 'scipy'"
```bash
pip install scipy scikit-learn -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q: 检测速度变慢
- RANSAC迭代次数从100降到50
- 或禁用卡尔曼滤波（注释掉滤波部分）

### Q: 精度反而下降
- 检查 RANSAC threshold 是否过大，降低到5-8
- 检查滤波是否过度平滑，提高 alpha 到 0.7

### Q: 报警级别总是 critical
- 降低 `confidence_threshold` 到 0.3
- 检查图像质量

---

## 技术支持

如遇到问题：
1. 检查日志输出中的详细错误信息
2. 查看 `quality_metrics` 字典内容
3. 对比改进前后的拟合图
4. 联系开发团队

---

## 总结

- **方案A**: 适合新项目或测试环境，快速验证
- **方案B**: 适合生产环境，完整集成
- **方案C**: 适合高风险环境，稳妥过渡

建议：先在测试环境用方案A验证效果，确认提升后再用方案B集成到生产环境。
